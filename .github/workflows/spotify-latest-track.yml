name: Update README with latest Spotify track

on:
  schedule:
    - cron: '0 * * * *'  # Runs every hour
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-readme-with-spotify:
    name: Update README with latest Spotify track
    runs-on: ubuntu-latest
    steps:
      - name: Fetch latest Spotify track
        uses: gautamkrishnar/blog-post-workflow@v1
        with:
          feed_list: "https://owono2001-dwf1a0r9k-owono2001s-projects.vercel.app/api/spotify"
          max_post_count: 1
          comment_tag_name: SPOTIFY-TRACK